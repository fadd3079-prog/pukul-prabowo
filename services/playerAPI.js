// api/playerAPI.js (frontend)&#10;&#10;import gameState from '../game/gameState.js';&#10;&#10;// ===== RETRY UTILITY =====&#10;async function retryFetch(url, options = {}, retries = 3) {&#10;  for (let i = 0; i < retries; i++) {&#10;    try {&#10;      const res = await fetch(url, options);&#10;      if (res.ok || i === retries - 1) return res;&#10;      await new Promise(r => setTimeout(r, 1000 * (i + 1)));&#10;    } catch (e) {&#10;      if (i === retries - 1) throw e;&#10;      await new Promise(r => setTimeout(r, 1000 * (i + 1)));&#10;    }&#10;  }&#10;}&#10;&#10;// ===== CONFIG =====&#10;const getBaseUrl = () => window.location.host.includes('localhost') ? '' : 'https://pukul-prabowo.vercel.app';&#10;&#10;export async function savePlayerScore(playerData) {&#10;  try {&#10;    const res = await retryFetch(`${getBaseUrl()}/api/players/score`, {&#10;      method: 'POST',&#10;      headers: { 'Content-Type': 'application/json' },&#10;      body: JSON.stringify({&#10;        name:     playerData.name,&#10;        province: playerData.province,&#10;        score:    playerData.score,&#10;      }),&#10;    });&#10;    if (!res.ok) throw new Error(`HTTP ${res.status}`);&#10;    const data = await res.json();&#10;    return { success: true, rank: data.rank || 0 };&#10;  } catch (e) {&#10;    console.warn('Gagal menyimpan skor:', e);&#10;    return { success: false, rank: 0 };&#10;  }&#10;}&#10;&#10;export async function getPlayerRank(name, province) {&#10;  try {&#10;    const params = new URLSearchParams({ name, province });&#10;    const res = await retryFetch(`${getBaseUrl()}/api/players/rank?${params}`);&#10;    if (!res.ok) throw new Error(`HTTP ${res.status}`);&#10;    const data = await res.json();&#10;    return data.rank || 0;&#10;  } catch (e) {&#10;    console.warn('Gagal mengambil rank:', e);&#10;    return 0;&#10;  }&#10;}&#10;&#10;export async function submitScore() {&#10;  if (!gameState.hit || gameState.hit <= 0) return;&#10;&#10;  const result = await savePlayerScore({&#10;    name:     gameState.player.name,&#10;    province: gameState.player.province,&#10;    score:    gameState.hit,&#10;  });&#10;&#10;  if (result.success) {&#10;    gameState.player.rank = result.rank;&#10;  }&#10;&#10;  return result;&#10;}&#10;
